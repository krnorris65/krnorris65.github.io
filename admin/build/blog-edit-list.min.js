/*! krnorris65.github.io 2017-11-15 */

const editEl=document.getElementById("blog_editContent"),blogDatabase=JSON.parse(localStorage.getItem("adminDatabase")).blog;if(blogDatabase.length>0){const e=document.createElement("h1"),t=document.createTextNode("Edit an Existing Blog");e.appendChild(t),editEl.appendChild(e),editEl.classList.add("hideContent");let a="";blogDatabase.sort((e,t)=>t.id-e.id).forEach(e=>{a+=`\n            <li>${e.title} <a id="edit_${e.id}"href="#">Edit</a></li>\n\n        `});editEl.innerHTML+=a}