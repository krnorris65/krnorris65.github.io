/*! krnorris65.github.io 2017-11-16 */

const editEl=document.getElementById("blog_editContent"),blogDatabase=JSON.parse(localStorage.getItem("adminDatabase")).blog,editContent=e=>{const t=parseInt(Array.from(e.target.classList).find(e=>{if(e.startsWith("edit_"))return e}).split("_")[1]);blogDatabase.filter(e=>t===e.id)};if(blogDatabase.length>0){const e=document.createElement("h1"),t=document.createTextNode("Edit an Existing Blog");e.appendChild(t),editEl.appendChild(e),editEl.classList.add("hideContent");const d=document.createElement("ul");editEl.appendChild(d);blogDatabase.sort((e,t)=>t.id-e.id).forEach(e=>{const t=document.createElement("li");t.appendChild(document.createTextNode(`${e.title} `));const a=document.createElement("button");a.appendChild(document.createTextNode("Edit")),a.className="edit_"+`${e.id}`,a.addEventListener("click",editContent),t.appendChild(a),d.appendChild(t)})}