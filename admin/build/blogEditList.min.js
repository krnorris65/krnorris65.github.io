/*! krnorris65.github.io 2017-11-15 */

const editEl=document.getElementById("blog_editContent"),blogDatabase=JSON.parse(localStorage.getItem("adminDatabase")).blog;if(blogDatabase.length>0){const e=document.createElement("h1"),t=document.createTextNode("Edit an Existing Blog");e.appendChild(t),editEl.appendChild(e),editEl.classList.add("hideContent");const d=document.createElement("ul");editEl.appendChild(d);blogDatabase.sort((e,t)=>t.id-e.id).forEach(e=>{const t=document.createElement("li");t.appendChild(document.createTextNode(`${e.title} `));const a=document.createElement("button");a.appendChild(document.createTextNode("Edit")),a.id="edit_"+`${e.id}`,t.appendChild(a),d.appendChild(t)})}